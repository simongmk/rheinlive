{
  "name": "rheinlive",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Busse und Bahnen in Köln, Bonn und Düsseldorf auf einer offenen Vektorkarte",
  "license": "MIT",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "start": "node scripts/dev.mjs",
    "build": "node scripts/build.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "node --check public/app.js && node --check public/map.js && node --check lib/transit.mjs && node --check lib/trip.mjs && node --check lib/api.mjs && node --check worker.mjs && node --check public/vehicles.js && node --check lib/network.mjs"
  }
}
